<% if @user.errors.count > 0 %>
  $('#errors-<%= @user.id %>').html('<%= escape_javascript(render("shared/error_messages", :formats => [:html])) %>')
<% else %>
    window.location.href='<%= admin_users_path %>'
<% end %>
